#include<stdio.h>
int main()
{
   int a,b,c;
   printf("enter the values :");
   scanf("%d%d%d",&a,&b,&c);
   if(a>b||b>c)
   {
      if(a>b)
      {
         printf("a=%d is large",a);
      }
      else
      printf("b=%d is large",b);
   }
   else 
   printf("c=%d is large",c);
}
