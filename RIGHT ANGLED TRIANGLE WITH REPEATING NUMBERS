#include<stdio.h>
void main()
{
   int numrow;
   int row,column;
   printf("enter the no of row");
   scanf("%d",&numrow);
   for(row=1; row<=numrow;row++)
   {
      for(column=1;column<=row;column++)
      {
         printf("%d",row);
      }
      printf("\n");

   }
}
